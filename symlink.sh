mkdir -p ~/.config/tms

ln -fs $(pwd)/nvim ~/.config
ln -fs $(pwd)/tmux/.tmux.conf ~/.tmux.conf
ln -fs $(pwd)/tmux-sessionizer/config.toml ~/.config/tms/config.toml
ln -fs $(pwd)/zk ~/.config
ln -fs $(pwd)/yazi ~/.config
